name: CI

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          cd starter
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest flake8 fastapi uvicorn httpx

      - name: Run flake8
        run: |
          flake8 starter/starter

      - name: Run tests
        run: |
          cd starter/starter
          pytest